# The substitutions table's header row becomes `| Use | Don't use | Meaning |` in all five vocabularies that carry one, and the prose pointing at the columns is conformed in all six — `Say` and `Don't say` were crude and read as speech, where a substitution binds names, prose, comments, commit messages, logs, and dialogue alike, which each table's own intro already said; `Meaning` was already in use for the same mixture of definition, argument, and STE note in design-by-efferent's terms table, headed `| Term | Role | Meaning |`, so the two tables in that file now agree on their last column; `Reason` and `Note` were evaluated and refused, `reason (n)` being unapproved with `CAUSE (n)` given for it and false on every row that argues nothing, and `Note` naming no thing while colliding with the STE note inside most cells
