# The self-executable rule carries the `curl` exception — the executable bit is set on every script without exception, including one fetched with `curl`, and what varies is the usage line: a script delivered by `git clone` or `git subtree` arrives executable and is invoked as itself, while a script delivered by `curl` never can be, because an HTTP response carries content and not file metadata, so its usage line reads `sh name.sh`
