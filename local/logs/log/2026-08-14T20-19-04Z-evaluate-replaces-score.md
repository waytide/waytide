# `evaluate` and `evaluated` replace `score` and `scored` in the `language` vocabulary, for judging a thing against a criterion and reaching a verdict — the substitution is domain-less rather than the experiment record's, so it sits beside `examine`, which it is fixed against: examining is inspecting to understand and evaluating reaches a verdict; `evaluate (v)` is not approved and the STE dictionary gives EXAMINE (v), so the word is reserved as a technical verb in the ETE deliberation-and-speech category, no STE verb category reaching a verb whose object is a judgment; `score` was the more expensive of the two, since the approved `SCORE (TN)` is a groove in a surface and a reader meeting it believes they know the word
