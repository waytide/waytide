# A publish is never offered while an experiment or a feature is open, and publishing waits for `master` rather than for any upstream branch — the rule is renamed `a-package-is-published-from-master`, a merge into another topic branch no longer licenses a publish, the concluded state is affirmed for an experiment and completed for a feature, since a feature has no affirmation, and the never-offer instruction does not reach an experiment whose own question is about publishing
