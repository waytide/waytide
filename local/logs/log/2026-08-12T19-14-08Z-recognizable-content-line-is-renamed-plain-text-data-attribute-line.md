# `recognizable-content line` is renamed `plain text data attribute line` — the form is used throughout the corpus in lines such as `**State:** Suspended` and `**Waits for:** the package publish`, which `session-start.sh` parses, and the old name was cited in only three live places against hundreds of lines written in the form; the three citations are corrected, and the records under `local/` that name the old term keep it as written
