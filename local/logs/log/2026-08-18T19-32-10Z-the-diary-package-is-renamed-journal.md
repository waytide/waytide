# The `diary` package is renamed `journal`, and the artifact with it — `system/diary/` becomes `system/journal/`, `local/diary/` becomes `local/journal/`, and `journal` is the term throughout Waytide
