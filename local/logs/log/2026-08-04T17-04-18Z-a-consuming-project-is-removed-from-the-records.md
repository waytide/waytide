# A downstream consuming project is removed from every record that named it — two decision-log entries, a feature record, two work session records, the plan-fold migration record, and the `code/ruby` gem-naming illustration, which was the only one distributed to users' machines; the records stayed true by dropping to a lower resolution rather than by being made to say something false
