# The status line gains an `untracked files` segment beside `uncommitted changes`, and an untracked file raises both — `uncommitted changes` names the axis, work not in the history, so narrowing it to tracked modifications would make the two a partition and force a developer scanning for whether anything is uncommitted to read two segments to answer one question; the new segment names the state whose remedy differs, since an untracked file is added or ignored where a modified file is committed, and the line had been reporting uncommitted changes over a tree where nothing was modified at all
