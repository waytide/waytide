# The `suspending DBE` substitution and its replacement note are removed — the experiment-lifecycle bullet recording that unattended replaced the phrase, the `attended-or-unattended-is-chosen-at-the-start` instruction not to use it, and the design-by-efferent vocabulary's substitution row, which existed only because of that history and without it ruled against a phrase nobody writes
