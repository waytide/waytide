# All seven packages are published from `master` after the ETE experiment merged — `report-direct-commits.sh` and `report-planning-directories-named-in-part.sh` both passed first, and every split fast-forwarded its component repository, so no force was needed and nothing diverged; the component repositories had been behind since the `both`-pass repairs on 2026-08-10, and the publish carries the ETE rules, the sixty vocabulary entries that name their categories, and the ten corpus-wide substitutions
