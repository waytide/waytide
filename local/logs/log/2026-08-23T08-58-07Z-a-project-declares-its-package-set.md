# That a project declares its package set — `package set: content` — and the packages outside it deactivate is recorded as an idea, supplying the installed-but-not-active mechanism the system has never had; it runs against the unconditional session-start read, and the distinction that makes it defensible is that a declared set is a recorded project decision rather than the in-the-moment judgment that rule refuses
