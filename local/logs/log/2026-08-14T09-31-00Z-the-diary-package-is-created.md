# The `diary` package is created — `waytide/local/diary/` holds the one record the engineer writes rather than the agent, read at session start after the rules and the vocabularies and followed in nothing; four decisions settled it through the selection UI (read at session start, committed under `waytide/local/`, binds nothing, one file per entry with the ISO-8601-UTC prefix), and two of them run against the experiment's forecast items 1 and 3; the package declares `foundation`, and two overclaims in foundation were narrowed to admit a session-start read that does not bind — rules-convention's "the one thing there that is" became "that binds", and the session-start hook's "Read only those two beside the packages" gained "unless an installed package's own rule directs another read"
