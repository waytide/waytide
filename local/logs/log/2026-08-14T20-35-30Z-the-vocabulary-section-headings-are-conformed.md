# The vocabulary section headings are conformed — design-by-efferent's `## Discontinued terms (do not use)` becomes `## Discontinuations`, which is the name the foundation vocabulary-convention gives that section and the name foundation's own vocabulary already used, and a heading carrying a parenthetical instruction was the crude form of it, its How to apply already saying not to reintroduce a discontinued term; foundation's `### Finer distinctions` moves above `## Discontinuations` to sit under Substitutions as it does in language and testing, since its one entry is about the substitution and the table's `see below` pointer had walked the reader past the discontinuations section to reach it
