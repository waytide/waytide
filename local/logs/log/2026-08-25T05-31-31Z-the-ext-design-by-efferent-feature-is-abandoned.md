# The ext/design-by-efferent feature is abandoned and nothing it produced reaches master by the engineer's instruction — this entry carries the substance, since no record of the work is on master to be read: the move would have taken system/design-by-efferent/ to system/ext/design-by-efferent/ in five increments, and it turned on a fork nobody settled, whether a new waytide/ext-design-by-efferent receives the package or the existing waytide/design-by-efferent is kept by a Repository declaration beside the Ruby package's; neither candidate makes the publish a fast-forward, because a subtree split's output history depends on the prefix path, which is what made this a migration rather than a rename; a project holding the package would have had to re-add it at the new prefix rather than git mv it, since git records a subtree by its prefix in a commit message and a move leaves that record naming the old path, reporting was never added on the next pull; the work was on feature/ext-design-by-efferent at f633590, deleted local and remote the same day and recoverable only from a reflog until it is collected
