# That an experiment works on an existing implementation and a feature is a new one is recorded as an observation, stated by the engineer as a prediction rather than a rule — it holds for most of the seventeen implementation records, and two run the other way: `diary` is an experiment that created a package that did not exist, and `work-sessions-rename` is a feature whose whole intent was renaming something that did
