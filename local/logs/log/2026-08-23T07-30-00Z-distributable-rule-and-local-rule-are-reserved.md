# `distributable rule` and `local rule` are reserved in the foundation vocabulary as technical nouns in the ETE agent artifacts category — `local` is the fourth word ETE fixes the meaning of, being approved in the STE dictionary as *applicable to a given location or position*, and both terms are held apart from `system rule`, which names where a rule sits rather than how it travels
