# `free-text option` is recorded as a term in the foundation vocabulary as well as a substitution, following the `efferent` and `actuation` precedent in the language vocabulary where a word that is both carries its definition and its STE note in the term and its substitution row points at it — the term says what the concept is, that the harness supplies it and the agent adds none of its own, that it keeps a closed list from foreclosing the answer only the engineer can produce, that it is not the `Explain` option, and that an option naming an answer inside the question is a candidate rather than a way out of the option set; the substitution row drops its STE note and points above, and the finer-distinction entry keeps the argument against the figure
