# `install` takes a list of names where a name is a package set or a package, proven by `test/automated/package-sets.sh` across three outcomes — no arguments installs the default set, `install content` installs the content set, and `install foundation git` installs those two; `default` is a set like any other rather than a branch in the code, and the installer carries the one repository name that is not its flattened path, since a package's `**Repository:**` declaration cannot be read before the package is installed
