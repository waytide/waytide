# That the Ruby tool package's installation approach is not sustainable, and that an install should take a package set on the command line, is recorded as an idea — a tool package currently declares the whole system as its dependency and carries an installer of its own, which does not compose when a second tool package arrives, and `refresh-packages.sh` already takes package names the same way
