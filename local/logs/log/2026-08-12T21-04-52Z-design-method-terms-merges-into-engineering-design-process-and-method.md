# The ETE category `Design method terms` merges into `Engineering, design, process, and method` — the two names both carried `method` and the line between them was already crossing in the assignments, with `loop` in one and `cycle` in the other though a cycle is one traversal of the loop, and `mean-bias` in one and `solubility` in the other though both name qualities the gates manage; the merged name enumerates, which is the idiom STE's own category names use, and eighteen entries take it; ETE now declares five categories, and a sixth, human-computer interaction, was declared for `attended` and `unattended`
