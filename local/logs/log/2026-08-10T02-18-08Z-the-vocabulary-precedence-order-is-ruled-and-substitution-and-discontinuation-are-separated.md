# The order the vocabularies decide in is ruled by foundation's vocabulary-convention, which also states which packages have a vocabulary and keeps substitutions and discontinuations under separate headings, so the declared-order-of-precedence deferred item is carried out and deleted — and its last residue is corrected here, `language`'s two `cut` rows and their finer-distinction entry now reading as substitutions rather than as discontinuations, so the merge overwrites `master`'s wording
