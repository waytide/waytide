# `read-consuming-projects.sh` is packaged in `foundation` rather than kept at the composite root — it is machine-scoped rather than an authoring tool, reporting the Waytide projects on one machine, and the developer who wants that is anyone who installed Waytide into more than one project rather than only whoever maintains Waytide; the cost accepted is a copy per project against one registry
