# The session-start hook carries the read instruction to the agent on `hookSpecificOutput.additionalContext`, separate from the developer-facing notice on `systemMessage` — the instruction states the read is unconditional, since the failure it answers was a small opening request judged not to warrant it; `WAYTIDE_QUIET` silences the notice but never the instruction, because a display preference must not disable the mechanism that carries the rules
