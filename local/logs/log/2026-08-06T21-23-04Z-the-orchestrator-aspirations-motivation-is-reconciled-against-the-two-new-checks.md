# The consuming-project orchestrator aspiration's Background & Motivation is reconciled — it claimed nothing addresses the last hop, which the two checks placed on 2026-08-06 made false in part, and it now states what those checks do not cover: a check inside a project reaches that project only when it refreshes, so the projects furthest behind receive it last and still have to be reached from outside
