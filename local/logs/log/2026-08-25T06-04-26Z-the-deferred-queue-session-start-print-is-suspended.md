# The deferred queue's session-start print is suspended — the queue predates the evolution of `ideas/` and the print spent a list at the head of every session on what has since become one tag among many; the print-the-deferred-queue-after-the-rule-read rule is removed, `session-start.sh`'s instruction drops the print clause and one `%s`, and the announce-waytide-at-session-start, a-record-list-is-a-table, deferred-queue-convention, and ideas-convention rules, foundation's README, and the root README each drop what cited it

**Tags:** [suspended]
