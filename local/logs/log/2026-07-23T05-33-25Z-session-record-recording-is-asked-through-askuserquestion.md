# When the agent asks whether to record a session, it uses the AskUserQuestion selection UI, offering three options: write a new session record, append the previous session record, or don't write one
