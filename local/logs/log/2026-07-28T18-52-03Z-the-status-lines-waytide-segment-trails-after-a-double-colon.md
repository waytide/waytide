# The status line's trailing Waytide segment is set off by a double colon rather than a hyphen — a hyphen also joins words, so it read as punctuation inside the sentence of segments instead of as the break between the developer's own state and the system standing behind it, and the double colon differs from the middle dot separating the other segments, which is what makes the trailing segment read as an annotation on the line rather than another item in it
