# The Journaling experiment is renamed Diary, and `diary` is the term for the artifact — `log` was taken by `waytide/local/log/`, `journal` collides with the write-ahead sense used in filesystems and databases, and `personal journal` needed a qualifier to separate it from that sense; the engineer chose `diary` over the agent's recommendation of `personal journal`, against the agent's argument that `diary` carries the British business sense of an appointment calendar, which points forward in time where a diary of the work points back
