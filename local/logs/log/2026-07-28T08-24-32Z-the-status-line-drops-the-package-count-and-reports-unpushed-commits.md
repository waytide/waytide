# The status line's Waytide segment is reduced to the single word `Waytide`, dropping the package count that told a developer nothing they act on and changed only on install while competing for width with the segments that do change — presence is still read from disk, only the display drops the number — and it gains an `unpushed commits` segment beside `uncommitted changes`, resolving the deferred item that gated it on three questions: the wording is words rather than a mark and distinct at a glance since both can show at once, it carries no count because the fact worth surfacing is that unpushed work exists at all, and a branch with no upstream reports nothing because `unpushed` is a claim relative to somewhere and there is nowhere — which leaves an accidentally-missing upstream unreported, a different fact deliberately not folded in; the segment reads only local refs and so reports the last known remote state, going silent when a stale tracking ref hides that the remote moved
