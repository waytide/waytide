# The Vocabulary substitution labels feature is started on `feature/vocabulary-substitution-labels`, branched from `master` at `4e586a2`, branch only and attended — it clarifies the substitutions table's column headers in the five vocabularies that carry one, `What it names` underselling a column that holds the definition, the reason, and the STE note; the section headings are out of scope, including the known disagreement between foundation's `Discontinuations` and design-by-efferent's `Discontinued terms (do not use)`, and no row's content changes
