# Foundation is published to `waytide/foundation` at `08981d2`, a fast-forward from `d0bffdb`, carrying the notice's `load waytide` command and dropped markdown emphasis, the `a few moments` caveat, the `untracked files` status-line segment, and the double colon setting off the Waytide segment — verified before pushing: no direct commits on any component repository, and the split's tree hash identical to the composite's `system/foundation`
