# The debt figure `owed` gives way to `required`, which is itself reserved — the agent put `Nothing further is owed` into the five vocabulary preambles while writing the rules that forbid figures, and into three headings of the experiment record; the eight live uses now say `required`, and the concluded STE experiment record keeps its words; the replacement is not approved either, since `required` is absent from the STE dictionary and `require (v)` gives NECESSARY (adj), which states a condition rather than what a rule asks, so `required` is reserved in ETE engineering, design, process, and method rather than adopted as approved
