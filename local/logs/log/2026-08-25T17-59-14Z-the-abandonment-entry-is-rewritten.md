# The ext/design-by-efferent abandonment entry is rewritten to the new rule — its five increments, its unsettled repository fork, and its account of what would be true between the increments are removed as unexecuted planning, and the git mv finding it carried is not lost, being already recorded on 2026-08-23 as a subtree's prefix is changed by removing the directory and re-adding it
