# A chosen mode is committed along with a matching decision-log entry, stated in the mode rule itself rather than left to the rules-convention and the decision-log-convention that already require both — the mode rule is what an agent reads at the moment a mode is chosen, and a rule saying only to write the file is followed exactly that far; an uncommitted mode rule is a mode that exists on one machine
