# The canonical `**State:**` line is added when a record reaches one of its state words, not when the experiment or the feature is started, and absence is the in-flight state — the two conventions each said both, which is why a record was written carrying `**State:** In flight` and the session-start notice reported the experiment as `(In)`; `session-start.sh` is unchanged, its empty-value branch having been correct all along
