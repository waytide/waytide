# **Conformed all seven component repos to the `master` branch, deleting `main`.** Eventide's standing norm is `master`, never `main` (the composite repo already used it). The component repos had been established on `main` by the earlier publishes and this session followed that; the norm makes that an inconsistency. Per repo: re-split (deterministic — the same commit `main` already pointed at), push to a new `master` branch, switch the repo's default branch to `master` (`gh repo edit --default-branch`), delete `main` (`git push --delete main`). No content changed — `master` points at the identical commit `main` did. Safe to delete `main` because there are no real consumers yet (only throwaway pull-proofs). **Tooling:** raw `git` for the splits/pushes/deletes on the keychain credential; `gh` only for the default-branch switch. **Docs conformed** `main`→`master`: the composite `README` install commands, and the Phase B runbook and checklist (which also gained a "Phase B executed 2026-07-17" status note — they now serve as the re-publish template). The runbook's language-reset narrative was reworded to say "the old `agent-norms-vocabulary` repo held" rather than assert a branch name, since that repo genuinely was on `main` at the time. Saved the norm to memory (`eventide-uses-master-not-main`). Final state: every repo — composite and all seven components — is `master`-only.
