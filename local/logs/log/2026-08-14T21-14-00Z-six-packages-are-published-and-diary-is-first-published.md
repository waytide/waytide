# Six packages are published from `master` — foundation, language, testing, design-by-efferent, and versioning fast-forwarded under the guard, and `diary` is published for the first time into a repository created for it, `waytide/diary`, described in the form the other seven use; `git` and `code/ruby` changed nothing since the 2026-08-14 publish of the other seven and are not re-split; the direct-commit report found no divergence before or after, so every component repository head traces to this history
