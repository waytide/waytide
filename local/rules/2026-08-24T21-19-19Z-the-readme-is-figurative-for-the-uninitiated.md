# The `README.md` is written for the uninitiated, and it is figurative where that serves them

**This project's `README.md` is outward-facing prose.** Its reader has not read Waytide's rules,
does not hold its vocabulary, and is deciding whether the system is worth their attention. **It is
written for that reader**, and it reaches for a figure where a figure is what lands.

**As the engineer stated it:** the README is often figurative — explaining to the uninitiated.
That's why it can sometimes appear to contradict Waytide's rules.

**So an apparent contradiction with a rule is not a defect there**, and it is not reported as one.

## What this reaches

**The root `README.md` of this repository.** That is the document the rule is about.

**It does not reach a rule file, a record, a commit message, an artifact under `local/`, or the
agent's answers.** Those are read by someone who has the vocabulary, or who is about to be bound by
it, and the `language` package governs them unchanged.

**A package's own `README.md` is not settled here.** Each is read by an engineer choosing whether to
install that package, which is the same position, and each is also the package's own summary of its
rules. This rule was written for the root document and does not claim the others.

## What is deliberately given up

**The `language` package's rules reach outward-facing prose and this takes an exception to them.**
The no-slang rule says its standard governs prose **and** conversation, on the ground that the
mindset it preserves lives in the person rather than in the artifact. The
waytide-prose-conforms-to-the-ste-writing-rules rule binds Waytide's prose, its records, its commit
messages, and the agent's answers. Neither carves out a document written for someone who does not
yet hold the terms.

**The figures already in the README are the worked cases.** *A free-text escape hatch* uses a word
the `foundation` vocabulary substitutes away — `escape` pictures a closed room, and the option set is
not closed. *The engineer takes the helm* is a figure for what the corpus calls responsibility
transferring. Each is precise enough for a reader meeting the system for the first time, and each
would be wrong in a rule.

**The cost is that the README stops being an example of the system's own standard.** A reader who
adopts Waytide from its README and then meets the vocabulary finds the two in tension, and nothing
in the README says why. That is accepted rather than solved.

## What is not licensed

**Not a false statement.** A figure stands in for a precise term. It does not stand in for a
different fact. *An agent memory store is fed by the committed artifacts* is not a figure for the
project-context-lives-in-the-committed-artifacts rule's *do not create one* — it says something
else, and this rule does not cover it.

**Not slang in place of an available plain word.** The exception is for the figure that **explains**.
A word reached for because it sounds better is the thing the language-is-precise-here rule calls
dramatic jargon, and it obscures for the uninitiated reader as much as for anyone.

**Why:** the terminology is the conveyable output of this work, and a reader who does not have it
yet cannot be conveyed to in it. A README written strictly to the vocabulary would define its terms
before it could describe anything, which puts the cost of the whole glossary in front of the first
paragraph. The reader has not decided to pay it, and that decision is what the README is for.

The rule
exists because the alternative is an agent auditing the README against binding prose every time it
reads one, and reporting the same findings the engineer has already declined. That happened, which
is what occasioned this.

**How to apply:** write the root `README.md` for a reader who holds none of Waytide's terms. Use a
figure where it explains better than the exact word does.

Do not report a README figure as a
violation of the `language` rules or a package vocabulary. Do report a statement that is **false**,
which this rule does not reach.

Hold every other artifact to the `language` package unchanged.

Related:

- the `language` no-slang rule — which reaches prose and conversation, and which this takes an
  exception to
- its language-is-precise-here rule — dramatic jargon, which is not licensed here
- its waytide-prose-conforms-to-the-ste-writing-rules rule — the standard the README is excused from
- the `foundation` vocabulary — `free-text option` rather than *escape*, the worked case
- the `foundation` ask-at-once-when-a-preference-is-expressed rule — the ask this rule came from

---

Authored by Scott Bellware on Mon Aug 24 2026 at 2:19:19 PM PT
