# Migration records

The design and execution history of migrating [`constant`](https://github.com/eventide-project/constant)'s `agent/rules/` into the Waytide packages. Kept for the record; not needed to *use* the packages (see the repo-root `README.md`) or to author them (see `CONTRIBUTING.md`).

- **`…-packaging-design.md`** — the design of record: package types, dependency graph, workflow, settled decisions.
- **`…-classification-table.md`** — every source rule pinned to its package, with merges and `local/` flagged.
- **`…-state-of-the-work.md`** — a summary of where the work currently stands.
- **`…-<package>-migration-plan.md`** — the per-package execution plan for each increment.
- **`…-phase-b-runbook.md`** / **`…-phase-b-checklist.md`** — the distribution (publish) procedure; still the template for re-publishing a package.
