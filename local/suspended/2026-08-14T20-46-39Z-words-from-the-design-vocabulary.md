- **Origin:** waytide/system/language/vocabulary.md — the `## Words from the design vocabulary`
  section, and waytide/system/foundation/vocabulary-convention.md — the fourth bullet of `What a
  vocabulary holds` and the paragraph stating the test that separated the kind from a term
- **Kind:** vocabulary section, and the convention entry defining the kind it held
- **Suspended:** 2026-08-14T20-46-39Z
- **Reconciliations:**
  - waytide/system/language/vocabulary.md — the section is removed. Its one entry, `preserve`,
    moves into `## Terms` and gains the category every term entry names, which it did not carry
    here
  - waytide/system/foundation/vocabulary-convention.md — the `Words from the design vocabulary`
    bullet is removed from `What a vocabulary holds`, leaving three kinds. The paragraph beginning
    *A term and a word from the design vocabulary differ in one test* is removed with it, since
    the distinction it draws no longer has two sides
  - a restore returns the section, returns the two passages to the convention, and moves
    `preserve` back out of `## Terms`

**Reason for suspension.** The kind had one entry four days after it was created, and that entry
contradicted the test that put it there. The test is *does the vocabulary say what the word
means*, and `preserve` was filed here because it is "defined nowhere". Its entry opens *to keep
something that already exists, rather than putting it there or serving it*, which is what a
definition is.

**So the word was a term the whole time**, and what the section actually carried was a note about
the word's **origin** — that Waytide did not coin it. An origin is worth recording and it is not a
kind of entry. It reads in the term's own prose.

**The cost was a section in every vocabulary's shape**, and a heading that is a clause where every
other heading is a name.

**What would bring it back.** A second and a third word that are genuinely used in another field's
sense and defined nowhere in Waytide. One entry does not establish a kind, and the kind is what was
suspended rather than the word.

---

## Words from the design vocabulary

Words Waytide takes from the established vocabulary of **structural design and software design**,
because the extant term already says the thing exactly. **Waytide does not define these.** It uses
them in their own sense, and reserves them where the STE dictionary disputes them.

**This is what the language-is-precise-here rule asks for.** That rule holds that the precise word
is the one that already means exactly this. It is as often an established technical term
as a plain one. A word here is one of those, kept rather than coined.

- **preserve** — to keep something that already exists, rather than putting it there or serving it.
  A rule preserves a mindset. Clarity is preserved. **STE: not approved.** The STE dictionary gives
  PRESERVATION, a technical noun for a maintenance procedure, which does not carry this.

**A term Waytide defines belongs above, not here.** `load-bearing` comes from structural
engineering and `cradle` from a physical object, and `design-by-efferent` defines the two, so the two are
terms. The line is whether Waytide says what the word means.

---

## The convention entry that defined the kind

From `What a vocabulary holds`:

- **Words from the design vocabulary** — a word taken from the established vocabulary of structural
  design and software design, because the extant term already says the thing exactly. `preserve`.
  **A vocabulary does not define these.** It records that the project uses them in their own sense,
  and reserves them where the STE dictionary disputes them.

And the paragraph that followed the substitution-and-discontinuation distinction:

**A term and a word from the design vocabulary differ in one test: does the vocabulary say what the
word means?** `load-bearing` comes from structural engineering, and `design-by-efferent` defines it,
so it is a term. `preserve` is used in its own sense and is defined nowhere, so it is not.

---

Authored by Scott Bellware on Fri Aug 14 2026 at 1:46:39 PM PT
