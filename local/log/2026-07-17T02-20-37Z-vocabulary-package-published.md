# Published the vocabulary package to `eventide-project/agent-norms-vocabulary` (public) and proved the pull. Vocabulary is the cross-cutting phrasing discipline (`include: foundation`): two principle rules (`name-literally` relocated, `no-slang` authored from the principle half of `terminology-no-slang-mediates`) and nine one-substitution rules — four relocated (`normal-path`, `install-packages`, `convey-not-thread`, and `sent-to` with its Eventide-attributed Why genericized) and five split out of `no-slang-mediates` (mediates/wrap with its `Constant` example genericized, conform/sweep, plain/land, scenario/arm, increment/cut). One rule per substitute, no substitutes table. Only `literal constant` stays `constant`-local. `git subtree split --prefix=vocabulary` pushed to `main`; a throwaway `subtree add` landed all thirteen files at `agent/rules/vocabulary/` with no `log/`. Third increment after docs+git and foundation; `name-literally`'s soft cross-reference to DBE's solubility/lexicon is left for a later generalization pass.
