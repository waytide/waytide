# The script that checks a consuming project's rules against Waytide's current vocabulary is placed in the `foundation` package, so each project runs it on itself — which closes the consuming-project orchestrator aspiration's reopened no-status resolution in favour of leaving it unchanged, and accepts that a project furthest behind receives the check last, since a package reaches a project only when that project refreshes
