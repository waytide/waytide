# A project's own vocabulary lives at `waytide/local/vocabulary.md` — one file beside `rules/`, named for what it is and taking no datetime prefix, read at session start and binding, its terms deciding over every package's — which reached the read instruction in `session-start.sh`, the announce-waytide-at-session-start rule, the rules-convention, the file-names rule, the bootstrap `install.sh` generates, and this repository's own `AGENTS.md`
