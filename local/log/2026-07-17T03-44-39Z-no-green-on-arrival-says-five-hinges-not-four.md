# Corrected the `no-green-on-arrival-tests` rule's stale hinge count. It described "the four-hinge cycle (actuation → assertion → controls → implementation)"; the cycle is **five** hinges — actuation → observation → controls → implementation → naming, with naming deferred to the feature's close — per `hinge-cycle`, which the rule postdates. The rule now refers to the hinge cycle and its implementation hinge without restating the count, so the count lives in one place.
