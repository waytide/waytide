# The session-start notice asks the developer to type `begin` — the foil request, a message with no content of its own whose only purpose is to give the agent an occasion to act on the instruction the hook already carries — replacing the earlier line that named the read outright: because the agent never sees the notice, a contentless word leaves the hook as the sole source of the instruction, so a read that follows is attributable to it, an agent that never received it must ask what `begin` means rather than failing silently, and the publish gate's third observation becomes an ordinary session instead of one run against the mechanism
