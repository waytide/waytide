# The STE Spec experiment is initiated on `experiment/ste-spec`, branched from `experiment/simplified-technical-english`, branch only and attended — it asks what the ASD-STE100 specification says, and a forecast of the document's structure and content is what makes a descriptive question testable; it exists to remove the constraint the parent experiment's cycle 3 records, that no finding may rest on a specific clause
