# A script's usage line names the command that runs it, written from the directory that command is typed in — `./name.sh` only for a script in that directory, the path form for a packaged script run from a project root, and `sh name.sh` only for one delivered by `curl` — correcting a rule that stated `./name.sh` as universal, conflated the `./` prefix with self-execution when the executable bit and shebang are what produce it, asserted as fact a practice that never held, and left `name.sh` unexplained as a placeholder
