# The `language` package's `ste/` directory is renamed `ete/` — the directory holds Waytide's implementation rather than the standard, and its own rules say so, naming themselves Eventide Technical English; the six rule files keep their names, including the two that carry `ste` because they refer to the standard rather than to the directory, and the eight path references in the five vocabulary preambles, the `language` README, and the ETE vocabulary entry are corrected
