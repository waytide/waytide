# The publishing rule drops its version condition, since Waytide carries no version numbers — the rule required a version to be decided before a package is published, which imported a condition from the `versioning` package's the-next-version-is-chosen-by-the-engineer rule, a package rule that governs a consuming project versioning its packages rather than this project, which versions nothing and has no tags, no version files, and no changelog; the rule is renamed `a-package-is-published-from-the-upstream-branch`, it now says publishing follows a merge to the upstream branch, and it states that Waytide carries no version numbers so a later reader does not reintroduce the condition
