# `install.sh` places `.claude/commands/load-rules.md` in a consuming project, making `/load-rules` a third channel for the read instruction — weakest by design, since it fires only when the developer types it, but the wording then lives in one committed file rather than being retyped each session, and an explicit developer instruction leaves the agent no judgment about whether a small-looking request warrants the read; an existing command file is never overwritten, the ignore warning now covers every `.claude/` file the installer placed and recommends the three-line `.gitignore` form that keeps `settings.local.json` ignored, and the command forbids printing a package count so it cannot become the agent-printed notice the announce rule prohibits
