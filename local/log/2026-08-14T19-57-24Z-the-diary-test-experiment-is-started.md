# The Diary test experiment is started on `experiment/diary-test`, branched from `master` at `e88385d`, branch only and attended — it asks whether the diary works, the `diary` package having been affirmed without a diary ever being written, so its fourteen rules describe a record that does not exist and none has been checked against use; a seven-item forecast is committed before the first entry, predicting the failure is a gap between two rules rather than an error inside one, and that it falls where the datetime line, the tags, the summary, and the body meet
