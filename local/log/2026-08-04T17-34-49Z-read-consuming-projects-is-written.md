# `read-consuming-projects.sh` is written at the composite root, discovering the actionable set from `~/.config/waytide/consuming-projects.toml` with no parsing dependency and refusing a registry outside its documented subset
</content>
