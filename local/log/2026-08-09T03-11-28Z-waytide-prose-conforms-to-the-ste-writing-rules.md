# [STE] Two rules are added in `system/language/ste/` — Waytide's prose conforms to the STE writing rules, and a Waytide term overrides the dictionary where the alternative loses the meaning. Both come from phrases the agent wrote. In "swept across 20 files", `sweep` is discontinued for `conform` and STE's own `SCAN` carries neither meaning, `ACROSS` is approved only as from one side to the other, the voice is passive with a known agent, and a semicolon breaks STE Rule 8.1. In "Both checks green", the verb is omitted against STE Rule 4.2, `both` is written where the dictionary gives `THE TWO`, and `green` is written where the `testing` vocabulary gives `verified`
