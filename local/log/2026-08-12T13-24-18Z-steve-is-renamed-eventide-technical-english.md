# STEve is renamed Eventide Technical English, abbreviated ETE — Waytide's implementation of ASD-STE100 keeps its content and takes the Eventide Project's name, `say it in ETE` replaces `say it in Steve` as the instruction to write that way, and the term is declared in the language vocabulary as a technical noun in the official documents, standards, and guidelines category, since `eventide`, `technical`, and `english` are each absent from the STE dictionary
