# The `WAYTIDE_QUIET` opt-out is removed — the session-start notice, the status line's Waytide segment, and what the initialization-rule prints at the head of the read are no longer silenceable, because an option whose whole purpose is to make the system invisible while it is in force sits against every reason those surfaces exist; the read instruction itself was never silenced by it, so that guarantee is unaffected
