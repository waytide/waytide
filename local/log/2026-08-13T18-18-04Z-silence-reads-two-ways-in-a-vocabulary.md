# Silence in a vocabulary entry reads two ways, since a definition and a substitution are different kinds of entry — a definition with no STE note names a term absent from the STE dictionary and reserves it, because a definition exists to name a term and a term names something in the subject field, while a substitution's Say word with no STE note is ordinary vocabulary, which is not reserved and not rewritten; the single default that read every silent entry as reserved forced `required` and `justified` into categories that did not fit, and the ordinary-vocabulary rule's own instruction to record no entry contradicted any status that would have recorded them
