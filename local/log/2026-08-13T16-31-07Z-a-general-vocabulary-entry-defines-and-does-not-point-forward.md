# A general vocabulary entry defines its term and does not point forward to the package that specializes it — the three doubled terms announced their specialization three different ways, with `efferent` naming `design-by-efferent` and restating what it adds, `actuation` restating the specialization word for word, and `solubility` saying nothing; each general entry now defines the term, states the dictionary's position, names its category, and stops, while each specialized entry keeps its pointer back to `language`
