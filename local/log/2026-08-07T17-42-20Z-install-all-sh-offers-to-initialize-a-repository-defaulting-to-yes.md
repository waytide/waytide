# `install-all.sh` offers to `git init` when the directory is not a repository, rather than refusing outright, and the prompt defaults to yes — a developer who fetched the script, chose a directory, and ran it has expressed the intention, `git init` destroys nothing, and defaulting it to no would sit oddly beside the empty initial commit the script already makes with no prompt at all; the prompt exists for the wrong-directory case, which the path printed above it is what catches
