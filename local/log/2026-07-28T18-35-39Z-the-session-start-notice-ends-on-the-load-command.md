# The session-start notice's closing sentences are swapped so the line ends on the command — `Waytide's rules are loaded before your first instruction will be processed. Loading the rules will take a moment. To load them now, type: load waytide.` — putting the loading-takes-a-moment caveat where the developer reads it before deciding and leaving the words to be typed as the last thing on the line, where the eye settles and where they can be copied without reading past them
