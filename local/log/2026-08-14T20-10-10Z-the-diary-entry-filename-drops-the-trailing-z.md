# The diary entry filename form drops its trailing `Z`, becoming `YYYY-MM-DDTHH-MM-SS-<name>.md` — in ISO 8601 that character designates UTC and the stamp is local time, so every entry filename asserted something false while the sentence discounting it sat in three rules a reader has no reason to open; the date-only form settled an hour earlier already carried no `Z`, so the package was emitting two forms that disagreed; the foundation package's file-names rule keeps its `Z`, its stamps being UTC; found by the writer reading the first real entry filename, which is the second finding of the Diary test experiment
