# The session-start notice's load command loses its markdown asterisks — the harness renders `systemMessage` as plain text rather than markdown, so the `**load waytide**` written earlier the same day reached the developer with the asterisks literal, drawing the eye to punctuation instead of to the words; the line stands without emphasis, since the command already ends the sentence that points at it, and the status line's bold repository name is unaffected because it is a terminal escape sequence rather than markup
