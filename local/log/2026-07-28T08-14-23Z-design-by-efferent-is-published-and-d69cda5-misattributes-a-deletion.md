# `design-by-efferent` is published to `e36e284` — a fast-forward from `af9762f`, split tree verified identical to `system/design-by-efferent`, `auto-record-design-dialogue.md` confirmed absent — and it carries a known history defect published deliberately rather than rewritten: commit `d69cda5`, titled for the installed-not-loaded notice wording, also deletes `system/design-by-efferent/auto-record-design-dialogue.md`, which belongs to `179838f`; the deletion was staged by a `git rm` still sitting in the index when a mid-turn instruction to commit arrived, and `git commit` takes the whole index rather than the file list passed to `git add`, so the explicit list did not restrict it — rewriting was declined because foundation had already been published from this exact history and the fix would have forced a push to a component repo that is correct, which `CONTRIBUTING.md` reserves for adopting divergence
