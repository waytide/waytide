# An absent mode rule means the mode selection prompt is displayed at once, rather than the agent reporting that the mode has never been chosen and offering to ask — finding no mode rule is the occasion to ask, and announcing the occasion leaves the project uninitiated until a second instruction arrives; the mode rule also now says to write "ask" rather than "put", which is archaic in the sense of "pose"
