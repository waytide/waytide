# The foundation-publish gate is settled on one criterion — the rule read completes before the agent produces anything, no response to the developer and no change to the repository, with reading and enumerating free — retiring the two non-equivalent criteria the deferred item carried: "before the first tool call that touches the request" counts tool calls, so it turns on how work is batched, and "before the first Edit/Write/mutating Bash" would have scored the first cold session a pass, which is the exact failure the item recorded
