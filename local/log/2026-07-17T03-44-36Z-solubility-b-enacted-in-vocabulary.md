# **solubility (b) enacted.** Added a general `solubility` rule to the `vocabulary` package (how readily a unit dissolves into use — usable and transparent from the standpoint of what uses it, as simple as it should be and no simpler; judged from the use site), and repointed `name-literally-not-by-analogy`'s "solubility criterion" reference to it. **Form:** a rule file, not a `vocabulary.md` — a one-entry glossary is not a glossary, and the distinction from DBE's stays principled (a `vocabulary.md` is for an interlocking *system* of terms; a standalone term-with-meaning is a rule). **This was a defect fix, not only tidiness:** the published `vocabulary` package's `name-literally` rule cited `agent/rules/terminology/2026-06-20T21-12-34Z-tdd-lexicon.md` — an obsolete path no consumer of the package has. Its obsolete worked-promotion path to an `agent/observations/…` file went too, and its "Related: the TDD lexicon rule and the efferent-not-caller rule" is relabeled to the DBE vocabulary, where both now live. The rule's parenthetical naming *hinge*, *gate*, *cradle*, *mean-bias* as kept coined handles **stands** — naming a term defined in another package is an informational pointer, not a dependency. **`vocabulary` now requires a re-publish** (Phase B, gated).
