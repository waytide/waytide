# A hinge's option is the artifact rather than a label for it, so a lone candidate is shown the same way and never as an accept prompt; the observation reads the side effect the actuation produces, a returned value being one case of that rather than the rule; the implementation is on screen before the loop moves on from its hinge; and the participation question is asked as "Who decides?", naming no hinge
