# `install-all.sh` checks three things before installing anything — that it is in a git repository, that it is not the Waytide authoring source, and that a commit exists to add onto — the last by making an empty initial commit itself, because `git subtree add` resolves HEAD to test the working tree and a repository with no commits therefore failed twice, the second failure reporting modifications that did not exist
