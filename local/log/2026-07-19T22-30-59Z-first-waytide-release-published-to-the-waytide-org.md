# First Waytide release — published all repos to the `waytide` org. Pushed the composite to `waytide/waytide` (`master` at `b8eae77`), then `git subtree split --prefix=<package>` and pushed each of the seven packages to its own `waytide/<package>` repo (`foundation`, `language`, `testing`, `code-ruby` from `code/ruby`, `git`, `plan`, `design-by-efferent`) — all first pushes creating `master` on the empty repos. Verified every repo has `master` and proved the `foundation` pull into a throwaway project (14 files landed: the convention rules, README, and `install.sh`). This ships everything accumulated since the Agent Norms era: the rebrand, the "puts not lands" phrasing, the two new `language` rules, the `docs`→`plan` rename, and the framework bootstrap (`foundation/install.sh` + `install-all.sh` wiring). **Published while unlicensed** at Scott's direction — the repos are public but carry the "not yet licensed for use; all rights reserved; Eventide Common Interest License forthcoming" note (the build-in-the-open path). Raw `git` for every push; `gh` only created the repos earlier. The old `eventide-project/agent-norms-*` repos are left behind, superseded. Resolves the pending-release and bootstrap deferred items. Still open: write the ECIL and re-publish with the license in each package.
