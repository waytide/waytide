# The session-start notice closes with a line addressed to the developer — `The rules are not read yet — type: Read the Waytide rules` — stating the true state before asking, carrying no quotation marks because the notice is interpolated into JSON without escaping and a double quote would make the harness fail to parse it and the notice vanish silently; it concedes that the hook's instruction to the agent does not reliably produce the read, and makes the developer the primary path and the hook the fallback
