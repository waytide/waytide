# `free-text option` replaces `escape` for the answer the selection interface supplies beside the options, recorded as a substitution in the foundation vocabulary, which owns the interface through the present-every-prompt rule — the word is reserved as a technical noun in the ETE human-computer-interaction category, where the ete-declares-its-own-categories rule already named it as an example, a category being no standing permission; `escape` is a figure whose picture is a closed room, and the option set is not closed since the free-text option is always there, so the word dramatized an ordinary part of the interface as a way out of a trap, and `escape (v)` is not approved, the STE dictionary giving DEFLATE (v); the 18 selection-interface uses in six rule files are conformed, and the six `escape sequence` uses in announce-waytide-at-session-start and statusline.sh are left, being the right word
