# `install-all.sh` is documented as `sh install-all.sh` rather than `./install-all.sh` — it is the one script delivered by `curl`, which transfers content and not file metadata, so the copy a developer runs is never executable whatever mode is committed; the README's install procedure had failed at its second line, and the same reasoning was already recorded for `install.sh` on 2026-07-28 and never applied here
