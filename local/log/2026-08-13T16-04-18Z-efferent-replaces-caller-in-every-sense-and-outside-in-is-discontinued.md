# `efferent` replaces `caller` in every sense, and `outside in` is discontinued — the earlier carve-out that exempted the concrete party is removed, so the sixteen uses in `code/ruby` now read `efferent`, and `caller` appears only in the `language` vocabulary, where the entry names it as the common software-development patois for the view and the substitutions row names it as the discontinued word; `outside in` is dropped from all five places that carried it, since the phrase has been misinterpreted in the industry, and each now says the interface is designed from the use site; the general definition is also corrected, since it had the direction inverted, and now reads as the view that conducts outward from the unit doing the using to the unit being used
