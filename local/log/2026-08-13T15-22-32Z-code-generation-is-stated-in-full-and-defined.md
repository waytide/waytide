# `generation` in the design-by-efferent sense is stated in full as `code generation` and defined in that package's vocabulary — the word was used eighty times and defined nowhere, while `product generation` was defined in `versioning` with an unrelated meaning, so a reader resolved it from context every time; the ten uses outside `versioning` now say `code generation`, and the new entry names it as the code the agent writes straight through, the complement of deliberation and the substance of what is mechanical
