# Published the code/ruby package to `eventide-project/agent-norms-code-ruby` (public) and proved the pull. code/ruby is prescriptive Ruby style (`include: foundation, vocabulary`): six relocated style rules (no-prepositions, no-inline-method-call-arguments, optional-params-default-in-body, positional-default-no-spaces, include-domain-mixin-before-infrastructure, build-normalizes/new-is-strict — the intra-family-factory privilege reclassified from local to general), a newly authored general robustness rule (robustness lives at the class interface, not the instance — strict core, forgiving perimeter, perimeter at increasing distance up to an outer-module coercion function), and a generalized `lib-report` command. Dead `constant`-specific cross-references (agent/log, agent/design, old terminology paths) were trimmed; Constant code examples remain for a later pass. `string-outputs-permissive-inputs` stays `constant`-local as the constant-names application of the robustness principle. First package under a grouping namespace: composite dir `code/ruby/`, subtree prefix `agent/rules/code/ruby`, repo flattened to `agent-norms-code-ruby`. Fifth increment; only design-by-efferent remains.
