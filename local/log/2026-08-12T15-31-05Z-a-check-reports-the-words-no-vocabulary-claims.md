# A check reports the words a project's rule prose uses that the STE dictionary refuses and no vocabulary claims — `system/language/report-unclaimed-words.sh` reads the rule files and every vocabulary in reach, compares them against the ASD-STE100 dictionary, and reports the refused words in two sections, one for words refused in every part of speech and one for words approved in one part of speech and refused in another, which is STE Rule 1.2 and which no count can settle; it exits 2 and says the check did not happen where the dictionary is absent, since ASD's text is in no package
