# The `waytide/framework/` to `system/` and `local/` migration script is retired — the one-time move is done, so `migrate-to-system-and-local.sh` is removed along with every reference to it: the foundation README's migration section and the `raw.githubusercontent.com` URL it was fetched from, the refresh script's pointer to it, and the comment naming which scripts tolerate untracked files
