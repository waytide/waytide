# The `report-unreserved-words.sh` check is removed as convoluted — it reported the refused words that no vocabulary reserved, and its 396-word report could not separate the ordinary English that will never be reserved from the words nobody had examined, since the two states are identical in the corpus; addressing that needed a record of what had been examined and left, which the ordinary-vocabulary-is-not-reserved-and-not-rewritten rule forbids in its own words, and the experiment's evidence is against the check anyway, since every finding that held came from reading and every check that promised to replace reading failed
