# The consuming-project vocabulary checks are packaged as one new script plus a change to an existing one — `report-unrecognized-mode.sh` in `foundation` reports a mode rule naming a mode the installed rule no longer defines, and the AGENTS.md bootstrap comparison is folded into `refresh-packages.sh`, replacing the reminder it printed, because a refresh is the moment the bootstrap drift is created and the moment nobody has to remember
