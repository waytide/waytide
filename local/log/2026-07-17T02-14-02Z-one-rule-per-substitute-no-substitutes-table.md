# Decided one rule per substitute — no `substitutes.md` table (revising M3). Each say-X-not-Y phrasing substitution stays its own rule file, consistent with the one-rule-per-file convention and with `rules-install-a-mindset` (terminology rules are the purest stance-installing rules; a table would demote each from a rule to a row and drop its own `Why` and dating). M3 changes from "build a per-package substitutes table" to "relocate/split each substitution into its own rule": for `vocabulary`, the four already-standalone substitution rules relocate unchanged and `terminology-no-slang-mediates` splits into a `no-slang` principle rule plus one file per substitution (wrap→mediates genericized, sweep→conform, land→plain, arm→scenario, cut→increment); `name-literally` and `no-slang` are the two principle rules. The redundant-rule merges (M1 controls trio, M2 hinge trio) are unaffected — those are genuine duplication, not distinct substitutions. A cohesive glossary of terms-with-meanings (the DBE lexicon) may still be a `vocabulary.md`. Updated the vocabulary plan, classification table, packaging design, and handoff.
