# The license file sits at the composite root and in each of the seven package directories, eight copies in all — a file at the composite root reaches only this repository, since a `git subtree split` carries only what is under the package prefix, so a package's own copy is what becomes that component repository's root LICENSE and what travels into every consuming project; placeholders stating that Waytide is not yet licensed and that the Eventide Common Interest License is forthcoming are placed now, ahead of the text
