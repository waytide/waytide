# `origination escape` is renamed `the free-text option` and the bare term `origination` is discontinued for plain phrasing — the compound was a metaphor stacked on a nominalization, and the engineer who commissioned the vocabulary did not recognize either, which is what the solubility test asks about; the six uses of the compound now name the thing the selection UI actually supplies, the slogan `Provoke origination, not mean-bias` becomes `Make the human produce the answer, not approve one`, the vocabulary entry is removed, and `foundation`'s citation of it is corrected; `originate from` stays where it is ordinary English, and the `originate-blind` run name in the experiments-convention stays as the name of a past run
