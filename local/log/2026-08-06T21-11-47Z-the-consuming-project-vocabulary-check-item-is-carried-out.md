# The deferred item asking for a check that a consuming project is still in step with Waytide's vocabulary is carried out and deleted — the mode-rule check is `foundation`'s `report-unrecognized-mode.sh`, the bootstrap check is part of `refresh-packages.sh`, and the consuming-project orchestrator aspiration's reopened no-status resolution is closed in favour of leaving it unchanged
