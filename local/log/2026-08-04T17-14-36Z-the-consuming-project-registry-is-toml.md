# The consuming-project registry is TOML at `~/.config/waytide/consuming-projects.toml`, superseding the JSON decision once no parsing dependency was to be taken, and recovering the comments that say why a path is excluded
</content>
