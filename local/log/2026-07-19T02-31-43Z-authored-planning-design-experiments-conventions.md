# Authored the planning, design, and experiments conventions (resolving the 2026-07-18 deferred item) — foundation directory rules `agent-plans-convention`, `agent-design-convention`, `agent-experiments-convention`, `experiment-runs-on-its-own-branch`, and `docs` writing rules `plan-document-format`, `design-document-format`; experiments placed all-foundation, single-branch-worked-to-a-verdict as the default (runs optional), lifecycle states affirmed / refuted / inconclusive / abandoned / superseded (endings) plus suspended (pause), affirmation copies the log into `agent/log/` and merges only when there is implementation, branches named `experiment/<subject>` with the base recorded (no tags); drafted from `constant`'s worked examples, the `agent/sessions/` survey spun off to its own deferred item.
