# Re-homed the subject-first commit-message rule from `methodology/` into the **git** package (`git/2026-07-13T20-54-39Z-subject-first-commit-messages.md`) in the Agent Norms composite repo: it is a commit convention and belongs with the other commit rules, so the git package now carries four rules rather than three. Decided during the 2026-07-15 classification-table walk-through (the rule was mis-filed under `methodology/` at the source — a live case of the purpose-over-category framing) and enacted in the Phase A migration. A straight relocation, copied unchanged; the rule already cross-references the `git/` commit rules, so it reads as consistent in its new home.
