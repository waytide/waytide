# **Phase B executed — all seven packages published, each pull proved.** Ran the checklist end to end. **Re-published (fast-forward, guarded)** `foundation`, `testing`, `code/ruby`, `git` — each `git subtree split --prefix=<dir>` was checked with `merge-base --is-ancestor <remote-main> <split>` before pushing, so no force was needed or used. **`language`** — the component repo was renamed `agent-norms-vocabulary` → `agent-norms-language` (`gh repo rename`, GitHub-side only), then force-pushed: the composite path change (`vocabulary/`→`language/`) makes the split a new history, so the reset (`d70c568…7c84045 (forced update)`) was intended, not a fast-forward. The only force in the batch. **`design-by-efferent`** — repo created (`gh repo create`, public), first-published, `main` created fresh. **Proofs:** each package pulled into a throwaway repo via `git subtree add`; file lists, structural files, and `log/` presence matched expectations — foundation 8 rules/no log, language 14/log(1), testing 17/log(3), code/ruby 8/no log, git 4/no log, DBE 17 rules + `vocabulary.md` + log(5), `include: foundation, language, testing`. **Tooling:** raw `git` for every push and pull (macOS keychain credential); `gh` used only for the rename and the create — no `gh auth setup-git`, so git config was untouched. `docs` needed nothing (unchanged since its prior publish). The migration is functionally complete: seven packages authored, consolidated, conformed, and live.
