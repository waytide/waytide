# Renamed the "handoff" note to `state-of-the-work` (`log/2026-07-13-agent-norms-handoff.md` → `…-state-of-the-work.md`). "Handoff" signals a human workflow (passing work between people); the file is really a summary of where the work currently stands, so the name now says that. Chose `state-of-the-work` over `current-state` / `status` / `project-status`. Updated the file's own title and intro, and the live references (the `log/` README, the Phase B runbook and checklist, and two deferred items). Left as-is: the design-by-efferent rules' use of "handoff" (a different meaning — the gate handing a hinge to the human), the historical `agent/log/` entries, and the completed docs+git migration plan's references. This entry is the link between the old and new names.
