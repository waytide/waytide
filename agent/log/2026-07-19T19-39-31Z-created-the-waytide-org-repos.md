# Created the eight `waytide` org repos (empty, public) via `gh repo create`. Scott created the `waytide` GitHub org and chose **public** visibility (empty stub repos, so nothing is exposed yet). The repos: `waytide/waytide` (composite) plus `waytide/foundation`, `waytide/language`, `waytide/testing`, `waytide/code-ruby`, `waytide/git`, `waytide/plan`, `waytide/design-by-efferent` — all confirmed empty (`isEmpty=true`) so the first push lands cleanly on `master`. **Nothing is published yet** — the actual publish (push the composite to `waytide/waytide`, `git subtree split` each package into its repo) stays gated and is held until the license is ready (packages should not go out all-rights-reserved). See the pending-release deferred item.
