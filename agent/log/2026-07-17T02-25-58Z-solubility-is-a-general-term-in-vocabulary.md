# Classified solubility as a **general** design-quality term (how readily a unit dissolves into use — usable, transparent, as simple as it should be), belonging in the `vocabulary` package as a general term-with-meaning, not exclusive to the `design-by-efferent` lexicon. The DBE lexicon references solubility rather than owning it. This resolves the coupling flagged in the vocabulary review — `name-literally`'s reference to "the solubility criterion" is fine because solubility is general, not a downward dependency on DBE. Enacting it (adding a `solubility` term to the vocabulary package and repointing `name-literally` there, and having the DBE lexicon reference it) is pending — either a small vocabulary update now or folded into the DBE increment. Updated the packaging design (vocabulary and DBE bullets).
