# Published the testing package to `eventide-project/agent-norms-testing` (public) and proved the pull. Testing is the controls-based test-writing conventions (`include: foundation, vocabulary`): the test-writing rules, the two what-to-test rules (assert-error-message-only, do-not-test-the-platform), three substitution rules (the M1 merge "controls — never factories, fixtures, or arrange" plus verified and protect), and the generalized `test-report` and `test-tree` commands. The M4 fold was reversed — error tests are named "Fails"/"Doesn't fail" (their own rule), so `test-name-is-prefix` stays just the "Is" rule. `namespace-variable-suffix` stays `constant`-local. This is the first package to carry a `testing/log/` of its own — two rule-content decisions (the test triad's third phase is "observe"; error tests are "Fails"/"Doesn't fail") that shipped with the package, while migration/authoring decisions stayed in the composite log — validating the distributed-vs-undistributed log split. Fourth increment; the relocated test-writing rules keep `Constant` examples for a later generalization pass.
