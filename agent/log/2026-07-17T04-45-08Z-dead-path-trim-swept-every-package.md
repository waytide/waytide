# **Swept every package for dead `agent/**` paths** — the trim pass had only ever run on `code/ruby`, so the other published packages still carried `constant`-layout paths that resolve nowhere for a consumer. Beyond `testing`'s two: **`vocabulary`** — `normal-path-not-happy-path` cited `agent/rules/terminology/…-name-literally-not-by-analogy.md` (same package, dead path), and `name-literally` cited the `tdd-lexicon` path and an `agent/observations/…` worked-promotion record. **`foundation`** — `rules-install-a-mindset` cited `agent/rules/methodology/…-subject-first-commit-messages.md` (a path that was already wrong before the migration: the rule was re-homed to `git`), the `agent/rules/terminology/` folder as a set, and an `agent/observations/…` discovery record; it also named **`no-slang-mediates`**, a rule that no longer exists — the M3 split made it `no-slang` plus `say-mediates-not-wrap`. **`git`** — `subject-first-commit-messages` cited the `commit-says-task-is-done` path and located its siblings at `agent/rules/git/`. All now reference rules by name and packages by package name. `docs` and `code/ruby` were already clean. **The rule stands:** references by *name* are informational pointers and are kept, across packages and in both directions; only *paths* go dead, and only paths were trimmed. **`foundation`, `vocabulary`, `testing`, and `git` all require a re-publish** (Phase B, gated).
