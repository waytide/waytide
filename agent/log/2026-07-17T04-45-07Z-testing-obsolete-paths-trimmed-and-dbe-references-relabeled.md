# **Ran the obsolete-cross-reference pass on `testing`** — the pass that ran on `code/ruby` at publication but never here. Trimmed two obsolete `constant` paths: `test-block-is-assertion-only`'s `agent/rules/code/2026-06-28T07-56-10Z-no-inline-method-call-arguments.md` (the rule is still referenced, by name), and `tdd-test-structure`'s "`test/automated/define_constant.rb` is the exemplar" (no consumer has the file). Relabeled the stale DBE references the genus test left behind: "the TDD-as-design-tool rule" → "the DBE-as-design-tool rule", "the TDD first-turn rule" / "the first-turn rule" → "the DBE first-turn rule" (`tdd-test-structure`, `test-block-is-assertion-only`), and "the lexicon rules" → "the DBE vocabulary" (`say-verified-not-green`, `say-protect-not-guard`). **References across the include graph are kept, not dropped.** A `Related:` note naming a rule is an informational pointer, not a dependency — `testing` naming a DBE rule (or `code/ruby`'s) resolves for the reader who has it and is merely unfollowed for the reader who doesn't. Only *paths* become obsolete. An earlier pass dropped these references on the theory that a package must not reference outside what it includes; that was wrong and is reverted. **`testing` requires a re-publish** (Phase B, gated).
