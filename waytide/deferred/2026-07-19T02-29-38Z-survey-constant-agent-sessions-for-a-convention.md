# Survey `constant`'s `waytide/sessions/` and consider a sessions convention

While surveying `constant` for the plans/design/experiments conventions (2026-07-18), a fourth artifact directory was noticed and **not** characterized: `constant`'s `waytide/sessions/`. It may warrant its own Waytide convention — a foundation *directory* rule (what `waytide/sessions/` holds, naming) and a `plan` *writing* rule (how a session record reads) — parallel to the plans/design/experiments rules just authored.

Nothing is known about its contents yet; the survey was deliberately not done.

**Gated on:** finishing this project's (Waytide) current work first. Per Scott's direction (2026-07-18), **no `constant` work at the moment** — return to `constant` only after the Waytide work in flight is done. Surveying `waytide/sessions/` means reading `constant`, so it waits.

**Why:** the lead is worth keeping so a fourth artifact convention isn't forgotten, but acting on it now would mean `constant` work Scott has explicitly deferred. A parked item keeps the lead without pulling focus off Waytide.

**How to apply:** once the Waytide work is complete and `constant` work is back on the table, survey `constant`'s `waytide/sessions/` for its de-facto conventions, then decide whether a `sessions` convention is warranted (foundation directory rule + `plan` writing rule) or whether it's out of scope. Related: the agent-experiments-convention, the agent-plans-convention, and the agent-design-convention (the sibling artifact conventions).
