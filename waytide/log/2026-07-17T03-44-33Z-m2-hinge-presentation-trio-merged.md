# **M2 enacted.** Merged the hinge-presentation trio — `actuation-hinge-shows-contexts`, `assertion-hinge-shows-actuation`, `controls-hinge-shows-actuation` — into one DBE rule, `what-each-hinge-displays`: a table (at hinge N, show X) plus the two principles that generate it — **the actuation is the anchor at every downstream hinge** (a hinge is meaningful only in relation to the actuation) and **the contexts are shown once, at hinge 1 where placement is decided, and elided after**. The three sources each stated one row and then re-argued the shared reasoning to stand alone, cross-referencing each other constantly; no file owned the subject. The merge was **four files into two, not three into one**: `hinge-cycle` restated the whole table in its "How to apply", which would have reproduced the duplication M2 exists to remove — it now keeps the *sequence* and the acceptance mechanism and points at the companion for *display*. The trio's three "Why" sections collapse into the two principles with no loss.
