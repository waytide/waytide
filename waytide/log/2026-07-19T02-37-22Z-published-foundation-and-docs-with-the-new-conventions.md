# Published `foundation` and `docs` (Phase B re-publish) carrying the new planning/design/experiments conventions — `foundation` shipped `agent-plans-convention`, `agent-design-convention`, `agent-experiments-convention`, `experiment-runs-on-its-own-branch` (plus the README reflecting the new work-artifact directories); `docs` shipped `plan-document-format`, `design-document-format` (plus README). Both were clean `git subtree split` fast-forwards to `master` (`agent-norms-foundation` 488c600→503d688, `agent-norms-docs` e990d90→04d99b3), each proved by a throwaway pull. No other package changed.
