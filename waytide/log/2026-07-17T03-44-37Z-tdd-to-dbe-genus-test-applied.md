# **Applied the TDD → DBE genus test across the DBE package.** Per the resolved conformance question: "TDD" is kept only where it names the general practice (the genus); anything labeled TDD that is DBE-specific is relabeled. Relabeled (built on the cradle/solubility/efferent/hinge machinery): `tdd-as-design-tool` → `dbe-as-design-tool`, `tdd-first-turn-test-file` → `dbe-first-turn-test-file`, `tdd-human-in-the-loop` → `dbe-human-in-the-loop`, `auto-record-tdd-dialogue` → `auto-record-design-dialogue`; `tdd-lexicon` → the DBE `vocabulary.md`. **Kept TDD:** `tdd-designs-coverage-protects-never-break-implementation` — it names the general red-green design cycle DBE builds on, and the distinction it draws (design vs. coverage) is what the DBE name's *Design* refers back to. Also enacted the queued conformance of the retired name **"efferent-oriented design" → Design By Efferent** in the two rules the deferred item named (`dbe-as-design-tool`, `first-implementation-may-run-a-contained-red-green-loop`); per that item's scope note, "efferent-oriented" as an adjective for *code* is not the methodology's name and stands. The deferred item itself lives in `constant` and still covers `constant`'s own observations, design docs, and README — not closed by this.
