# A sessions convention is established: waytide/sessions/ holds the narrative record of a work session — a chronological guided tour that points to the durable records (rules, observations, log, experiments) as the source of truth. The agent-sessions-convention (foundation) defines the directory and when to write one; session-document-format (plan) defines how it reads. Grounded in constant's session record. The survey-constant-sessions deferred item is resolved
