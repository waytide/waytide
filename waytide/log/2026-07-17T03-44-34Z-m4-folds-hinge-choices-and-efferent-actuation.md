# **M4 folds enacted for DBE.** `hinge-choices-numbered-list` folded into `present-every-prompt-through-askuserquestion` (which already declared it generalizes it) — the absorbed content is the two-option floor with its "find the real underlying decision rather than padding" forcing function, and the instruction *not* to add your own "Chat about this" because the selection UI's built-in free-text "Other" **is** the mandatory origination escape. `tdd-efferent-not-caller` and `actuation-not-call` folded into the DBE `vocabulary.md` as its efferent and actuation entries (each keeping its own section, since each is a stance with a rationale, not just a table row). None of the three remains a separate file. **Consequence:** the fold resolved a live contradiction in the sources — `hinge-cycle` and `actuation-gate-options-or-chat` both instructed the AI to offer a "Chat about this" option of its own, which `hinge-choices` had superseded. Both are conformed to the single escape mechanism.
