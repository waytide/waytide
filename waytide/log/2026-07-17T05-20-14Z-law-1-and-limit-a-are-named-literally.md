# **Retired the opaque codes "Law 1" and "limit A", naming both results literally.** They were codes in the sense `name-literally-not-by-analogy` rejects outright — the reader must memorize that "Law 1" stands for one idea and "limit A" for another — and `design-by-efferent` includes `language`, so the rules were breaking a rule they carry. The codes also pointed at a gate-forecasting observation that does not travel with the package, so nothing could be looked up. **The literal names, now the only names:** **"provoke origination, not mean-bias"** (was Law 1 — why a gate presents alternatives rather than a single proposal) and **"the best options are ones the AI can't produce"** (was limit A — why the origination escape is mandatory at every gate, without exception). Both are stated in full in the DBE `vocabulary.md` under *The two results the gates rest on*, and the four rules that leaned on the codes (`actuation-gate-options-or-chat`, `hinge-cycle`, `one-outcome-at-a-time`, `present-every-prompt`) now say the result instead of citing a code. Each already carried the gloss beside the code, so no content was lost — the code was pure toll. **Also added `mean-bias` and `origination` as vocabulary terms.** `mean-bias` is a coined handle `language`'s `name-literally` rule cites by name as kept "because defined plainly elsewhere" — and elsewhere was the undistributed observation, so it was defined nowhere a consumer could reach. It is defined here now, which is what makes keeping it legitimate.
