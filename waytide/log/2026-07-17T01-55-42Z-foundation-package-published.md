# Published the foundation package to `eventide-project/agent-norms-foundation` (public) and proved the pull. Foundation is the base every other package includes (`include: (none)`): four convention rules authored from `constant`'s `AGENTS.md` framework prose (`agent/rules`, `observations`, `deferred`, `log`), plus four relocations — `rules-install-a-mindset` and `next-deferred-item` unchanged, `agent-file-names-iso8601` amended with the `package.md`/`README.md` exemption note, and `status-report` generalized (Constant specifics and the memory-retired part dropped, the suite section made convention-aware of the testing package). The README doubles as the package's umbrella framework overview. `git subtree split --prefix=foundation` pushed to `main`; a throwaway `subtree add` landed all ten files at `agent/rules/foundation/` with names intact and no `log/` (it starts empty, per the rule-content-only log principle). Second increment after docs+git.
