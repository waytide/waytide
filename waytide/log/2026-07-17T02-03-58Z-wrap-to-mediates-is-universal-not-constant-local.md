# Corrected the classification of the wrap→mediates substitution: it is **universal**, not `constant`-specific, so it belongs in the general `vocabulary` package's `substitutes.md`, not the `local/vocabulary/` mirror. "mediates" is the general verb for a mediating/accessor relationship; only the `terminology-no-slang-mediates` rule's *example* ("a `Constant` mediates a module") is `constant`-specific, and it is genericized on migration. This reverses the earlier "mediates → local/vocabulary" note in the classification table and packaging design; the only genuinely `constant`-domain vocabulary term remaining in `local/vocabulary/` is "literal constant". Updated the vocabulary increment plan, the classification table (row + local/ tally), and the packaging design.
